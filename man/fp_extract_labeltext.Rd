% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fp_extract_labeltext.R
\name{fp_extract_labeltext}
\alias{fp_extract_labeltext}
\title{Extract label text columns}
\usage{
fp_extract_labeltext(x, ..., cols = NULL, names = NULL, na = "")
}
\arguments{
\item{x}{A data frame (grouped or ungrouped).}

\item{...}{Columns selected using tidyselect syntax.}

\item{cols}{Optional explicit character vector of column names. Cannot be
combined with \code{...}.}

\item{names}{Optional replacement names for the extracted columns.}

\item{na}{Replacement value for missing values in atomic scalar cells.}
}
\value{
A data frame suitable for \code{labeltext} in \code{forestplot()}.
}
\description{
Build a labeltext data frame from selected columns. This helper supports
both tidyselect syntax and explicit character column names.
}
\details{
For grouped data frames, extraction follows the grouped forestplot row
alignment logic so that label rows correspond to the first grouped estimate
panel.
}
\examples{
data(inventors_vs_mello)

# ungrouped selection with explicit columns
fp_extract_labeltext(
    inventors_vs_mello,
    cols = c("author", "ai", "n1i", "ci", "n2i", "orci"),
    names = c("Study", "E1", "N1", "E2", "N2", "OR"),
    na = ""
)

# grouped extraction follows grouped forestplot row alignment
# (rename estimate columns to mean/lower/upper for grouped extraction)
inventors_vs_mello |>
    dplyr::rename(mean = est, lower = lb, upper = ub) |>
    dplyr::group_by(group) |>
    fp_extract_labeltext(author, orci, na = "")
}
