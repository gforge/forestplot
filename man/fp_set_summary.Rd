% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_styling.R
\name{fp_set_summary}
\alias{fp_set_summary}
\title{Selector-based summary rows}
\usage{
fp_set_summary(x, where)
}
\arguments{
\item{x}{A \code{gforge_forestplot} object.}

\item{where}{A row predicate. Supports:
\itemize{
\item a bare expression evaluated in source data columns (when available), e.g.
\code{startsWith(type, "Subtotal") | startsWith(type, "Total")}
\item a one-sided formula, e.g. \code{~ Type \%in\% c("header", "subtotal")}
\item a function receiving selector data and returning a logical vector
\item a logical scalar/vector
}}
}
\value{
A modified \code{gforge_forestplot} object.
}
\description{
Set \code{is.summary} rows on a \code{gforge_forestplot} object using a row predicate.
This helper is intended for pipe-first workflows where summary rows are
decided after label extraction/remapping.
}
\examples{
data(inventors_vs_mello)
inventors_vs_mello |>
  forestplot(mean = est, lower = lb, upper = ub, labeltext = author) |>
  fp_extract_labels(Study = author) |>
  fp_set_summary(startsWith(type, "subtotal") | startsWith(type, "total"))
}
